# Supabase — em dev local, estes valores saem de `npm run db:start` (supabase start).
# Copie este arquivo para `.env` e preencha. O `.env` NÃO vai para o git.
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=cole-aqui-o-anon-key-impresso-pelo-supabase-start
